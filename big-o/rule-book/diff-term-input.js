function compressBoxesTwice(boxes, boxes2) {
  boxes.forEach(function (box) {
    console.log(box);
  });

  boxes2.forEach(function (box) {
    console.log(box);
  });
}

/*
  If the function do have multiple inputs, then you can say that this function has a
  Big O(a + b)
*/